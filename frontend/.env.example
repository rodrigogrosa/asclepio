# URL base da API FastAPI (contrato v1)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Modo MOCK: "true" usa fixtures locais (lib/mock) para todos os endpoints, sem backend
NEXT_PUBLIC_USE_MOCK=false
